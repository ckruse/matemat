<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
  <span class="icon-bar"></span>
  <span class="icon-bar"></span>
  <span class="icon-bar"></span>
</a>

<a class="brand" href="<%= root_url %>">Matemat</a>
<div class="nav-collapse">
  <ul class="nav">
    <% if current_user %><li<% if controller_path == 'admin/users' %> class="active"<% end %>><%= link_to(t('views.administration'), admin_users_path) %></li><% end %>
  </ul>

  <ul class="nav pull-right">
    <% if current_user %>
      <li<% if controller_path == 'users' and @user and @user.user_id == current_user.user_id %> class="active"<% end %>><%= link_to current_user.display_name, edit_user_path(current_user) %></li>
      <li><%= link_to('Logout', logout_path) %></li>
    <% else %>
      <li><%= link_to('Login', login_path) %></li>
    <% end %>
  </ul>
</div>
