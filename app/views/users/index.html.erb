<table class="table table-striped table-bordered">
  <thead>
    <th>Name</th>
    <th>Guthaben</th>
    <th>Letzte Aktivität</th>
  </thead>

  <tbody>
    <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.display_name, user_path(user) %></td>
      <td><%= @bookings[user.account.account_id] || 0 %> CHF</td>
      <td><%= @activities[user.account.account_id] ? localize(@activities[user.account.account_id], format: "%d. %B %Y %H:%M") : "(nie)" %></td>
    </tr>
    <% end %>
  </tbody>
</table>
